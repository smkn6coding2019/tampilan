<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.tampilan">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
    </activity>

        <activity android:name=".SearchObatCairActivity">
        </activity>

        <activity android:name=".SearchP3KActivity">
        </activity>

        <activity android:name=".SearchObatRacikActivity">
        </activity>

        <activity android:name=".SearchObatOlesActivity">
        </activity>

        <activity android:name=".SearchObatPillActivity">
        </activity>

        <activity android:name=".SearchResepDokterActivity">
        </activity>

        <activity android:name=".SearchObatTetesActivity">
        </activity>

        <activity android:name=".SearchAlatDokterActivity">
        </activity>

        <activity android:name=".SearchMilkActivity">
        </activity>


    </application>

</manifest>